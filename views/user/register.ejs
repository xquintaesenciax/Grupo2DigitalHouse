<!DOCTYPE html>
<html>
  <%- include("../partials/head") %>

  <body>
    <%- include("../partials/header") %>
    <div class="login-container">
      <h2>Creá tu cuenta</h2>

      <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
      <div class="feedback" role="alert">
        <ul>
          <% errors.forEach(error => { %>
          <li><%= error.msg %></li>
          <% }) %>
        </ul>
      </div>
      <% } %>

      <form
        action="/user/register"
        method="post"
        enctype="multipart/form-data"
        class="form-register"
      >
        <div class="form-group">
          <label for="nombre">Nombre:</label>
          <input
            type="text"
            id="nombre"
            name="nombre"
            value="<%= (locals.old && locals.old.nombre) ? locals.old.nombre : '' %>"
          />
          <p
            class="feedback nombre"
            style="display: none; margin: 0px; padding: 0px; font-size: 12px"
          >
            Ingrese un nombre con más de 2 caracteres.
          </p>
        </div>
        <div class="form-group group-apellido">
          <label for="apellido">Apellido:</label>
          <input
            type="text"
            id="apellido"
            name="apellido"
            value="<%= (locals.old && locals.old.apellido) ? locals.old.apellido : '' %>"
          />
          <p
            class="feedback apellido"
            style="display: none; margin: 0px; padding: 0px; font-size: 12px"
          >
            Ingrese un apellido con más de 2 caracteres.
          </p>
        </div>
        <div class="form-group group-email">
          <label for="email">E-mail:</label>
          <input
            type="email"
            id="email"
            name="email"
            value="<%= (locals.old && locals.old.email) ? locals.old.email : '' %>"
          />
          <p
            class="feedback email"
            style="display: none; margin: 0px; padding: 0px; font-size: 12px"
          >
            Ingrese un email valido.
          </p>
        </div>
        <div class="form-group group-usuario">
          <label for="username">Usuario:</label>
          <input
            type="text"
            id="username"
            name="username"
            value="<%= (locals.old && locals.old.username) ? locals.old.username : '' %>"
          />
          <p
            class="feedback usuario"
            style="display: none; margin: 0px; padding: 0px; font-size: 12px"
          >
            Ingrese un nombre de usuario con más de 2 caracteres.
          </p>
        </div>
        <div class="form-group group-password">
          <label for="password">Contraseña:</label>
          <input
            type="password"
            id="password"
            name="password"
            value="<%= (locals.old && locals.old.password) ? locals.old.password : '' %>"
          />
          <p
            class="feedback password"
            style="display: none; margin: 0px; padding: 0px; font-size: 12px"
          >
            Ingrese una contraseña con más de 8 caracteres.
          </p>
        </div>
        <div>
          <label for="profile-pic">Foto de perfil:</label>
          <input type="file" id="profile-pic" name="profile-pic" />
          <p
            class="feedback img"
            style="display: none; margin: 0px; padding: 0px; font-size: 12px"
          >
            Ingrese una imagen con extenciones .JPG, . JPEG, .PNG o .GIF.
          </p>
        </div>
        <div class="form-group">
          <input type="submit" value="Crear cuenta" />
        </div>
      </form>
    </div>
    <%- include("../partials/footer") %>
    <script src="/js/validationRegisterUser.js" defer></script>
  </body>
</html>
